import styles from '@/app/ui/home.module.css';

export default async function BlogPost() {
    return (
        <section className={styles.section}>
            <h1 className={styles.heading1}>Blog Post</h1>
        </section>
    )
}